Updates on chapter 3 are as follows:

	- a function(checkHoveredBox(coordinates)) to check the current box being hovered on.
	- a function(drawCover(box)) to draw a cover on the hovered box.
	- a function (unCover(box)) to uncover the previously covered box.
	- declaration of additional variable constants.
	- implemented the MOUSEMOTION event.