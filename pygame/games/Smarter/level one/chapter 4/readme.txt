Updates on chapter 4 are as follows:

	- animation of the success or failure text.
	- checking if the user has given a right or wrong answer.
	- a function(levelFinished()) to check if the level has ended or not.
	- a function(nextQuestion()) to load the next question.
	- added the MOUSEBUTTONUP or MOUSEBUTTONDOWN events.